#!/usr/bin/env python

import sys

import poachplatelib.package

if __name__ == "__main__":
    sys.exit(poachplatelib.package.main(sys.argv) or 0)
    # try:
    #     sys.exit(poachplatelib.package.main(sys.argv) or 0)
    # except Exception, e:
    #     sys.stderr.write('%s\n'%str(e))
    #     raise e
    #     sys.exit(1)    

